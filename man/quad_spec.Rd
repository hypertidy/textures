% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quad-spec.R
\name{quad_spec}
\alias{quad_spec}
\alias{quad_mesh}
\alias{quad_mesh.quad_spec}
\alias{as.mesh3d.quad_spec}
\alias{print.quad_spec}
\title{Compact quad mesh specification}
\usage{
quad_spec(
  dimension = c(1L, 1L),
  extent = NULL,
  ydown = FALSE,
  crs = NULL,
  texture = NULL
)

quad_mesh(x, ...)

\method{quad_mesh}{quad_spec}(x, ...)

\method{as.mesh3d}{quad_spec}(x, ...)

\method{print}{quad_spec}(x, ...)
}
\arguments{
\item{dimension}{number of cells in the grid (nx, ny), a single value
is recycled}

\item{extent}{extent of the grid \code{c(xmin, xmax, ymin, ymax)}, default
is the unit square}

\item{ydown}{should the y coordinate be counted from the top, default
\code{FALSE}}

\item{crs}{optional coordinate reference system, stored but not used
by textures itself}

\item{texture}{optional file path to a PNG image to texture onto the
mesh when materialized}

\item{x}{a quad_spec object}

\item{...}{ignored, or passed between methods}
}
\value{
\code{quad_spec()} a list with class 'quad_spec', \code{quad_mesh()} a
mesh3d object, the print method returns its input invisibly
}
\description{
A quad mesh on a regular grid is completely determined by a tiny
recipe: the grid dimension, its extent, and the y orientation.
\code{quad_spec()} records that recipe as a plain, serializable list
without materializing any vertices or indexes.
}
\details{
\code{quad_mesh()} materializes the specification as a 'mesh3d' object
(as used by the 'rgl' package, but constructed without it). If the
spec carries a \code{texture} image file path, texture coordinates are
included: these are the extent-normalized vertex coordinates, so
they remain valid for any mesh whose vertices lie in the extent.

With the 'rgl' package installed, \code{as.mesh3d()} works directly on a
quad_spec (registered on-demand, 'rgl' is not required).

The mesh density given by \code{dimension} is independent of the pixel
dimension of any \code{texture} image: a coarse mesh may carry a
full-resolution image, the graphics engine interpolates within each
quad.

A spec can be stored, serialized, and sent where a materialized mesh cannot
sensibly be - materialization is deferred to the consumer.
}
\examples{
spec <- quad_spec(c(20, 10), extent = c(100, 160, -60, -30))
spec
mesh <- quad_mesh(spec)
str(mesh$vb)
}
\seealso{
Other textures:
\code{\link[=break_mesh]{break_mesh()}},
\code{\link[=quad]{quad()}}
}
\concept{textures}
