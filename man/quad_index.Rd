% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quad-core.R
\name{quad_index}
\alias{quad_index}
\alias{quad_edges}
\alias{quad_vertex}
\title{Quad mesh primitives}
\usage{
quad_index(dimension, ydown = FALSE)

quad_edges(dimension, extent = NULL, ydown = FALSE)

quad_vertex(dimension, extent = NULL, ydown = FALSE)
}
\arguments{
\item{dimension}{number of cells in the grid (nx, ny), a single value
is recycled}

\item{ydown}{should the y coordinate be counted from the top (image/raster
orientation), default \code{FALSE}}

\item{extent}{extent of the grid \code{c(xmin, xmax, ymin, ymax)}, default
is the unit square}
}
\value{
\code{quad_index()} a 4-row matrix of vertex indexes (integer, or
double for very large grids), \code{quad_edges()} a list with \code{x} and \code{y}
edge coordinate vectors, \code{quad_vertex()} a 2-column matrix of vertex
coordinates
@family textures
}
\description{
Generate the components of a quad mesh from the compact specification of
a regular grid: the \code{dimension} (number of cells nx, ny) and optionally
the \code{extent} (xmin, xmax, ymin, ymax).
}
\details{
\code{quad_index()} gives the index of the four corner vertices of every
cell, a 4 x (nx * ny) matrix of 1-based vertex indexes. Quads are in
cell order, x fastest (the \code{\link[=matrix]{matrix()}} and \code{\link[=image]{image()}} convention, and
raster cell order when \code{ydown = TRUE}). Winding is counter-clockwise
for y-up grids.

\code{quad_edges()} gives the unexpanded form of the vertices, the x and y
coordinates of the cell edges as two vectors of length nx + 1 and
ny + 1. This is the compact intermediate: the full vertex set is their
outer expansion, and for many purposes (transform bounds, axis
coordinates, texture alignment) the margins are all that is needed.

\code{quad_vertex()} gives the materialized vertices, a (nx + 1) * (ny + 1)
row matrix of x, y coordinates, x fastest, matching the index in
\code{quad_index()}.

For very large grids where the vertex count or index length exceeds
the integer maximum, \code{quad_index()} returns a matrix of doubles
(exact for integer values to 2^53), which R subsetting and 'mesh3d'
usage accept natively. Take care not to apply \code{\link[=as.integer]{as.integer()}} to such
an index.
}
\examples{
quad_index(c(2, 3))
quad_edges(c(2, 3), extent = c(0, 10, 0, 20))
quad_vertex(c(2, 3), extent = c(0, 10, 0, 20))
}
